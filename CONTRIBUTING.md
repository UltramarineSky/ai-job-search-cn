# 贡献指南

本仓库是面向中国大陆求职场景的个人工具。如果你 fork 了它、想在自己的副本上改东西，
下面是这个代码库的实际做法。

## 新增一个招聘平台（portal）

用 `/job-add-portal` 生成一个新的 portal 搜索技能，它会：

1. 交互式询问平台 URL、技能名（`<平台>-search` 形式）、目标城市/关键词
2. 调研该平台的搜索/详情页结构，记录到 `url-reference.md`
3. 按统一的 portal-skill 契约（`search`/`detail` 命令、`--format json|table|plain`、
   stderr JSON 错误 + 退出码 1）脚手架出 CLI
4. 用真实查询跑一遍 `search`/`detail`，确认返回的是可读数据而不是空字段或 HTML 碎片
5. 注册到 `.agents/skills/`，`/job-scrape` 会自动发现并纳入

`.agents/skills/liepin-search/` 是仓库里现成的零运行时依赖范例（`package.json` 里
`dependencies` 为空，`bun install` 只拉 TypeScript 类型），新增 portal 时优先照它的
结构抄。

## 改技能壳（`.claude/skills/*/SKILL.md`）

技能壳是薄的：`description` 管什么时候自动触发，`allowed-tools` 管跑起来时手里有
哪些工具，正文一句「读取并严格执行 `workflows/<名>.md`」把活交出去。

**壳归 Claude，正文归所有工具。** `.claude/` 底下不该有任何正文——别的 AI 工具按
`AGENTS.md`「工作流索引」直接读 `workflows/<名>.md`，读到 `.claude/…` 路径就走不下去了。
所以工作流正文里**不许出现 `.claude/` 路径，也不许自称「本技能 / this skill」**
（`tools/lint_skills.py` 扫这两类）。实测栽过：`job-scrape.md` 里那句「框架自己的
`search-queries.md` **在本技能目录下**」，在正文搬出技能之后指向了一个不存在的位置。

**改工作流时顺手看一眼壳。** `allowed-tools` 是手写的，工作流里新加一条要在 shell
里跑的命令，它不会自动跟上——而它跟不上的后果是那一步每次都要用户手批，或者干脆
跑不了。2026-08-18 实测两处：`job-scrape` 的工作流要跑四个 `python tools/*.py`、
`job-upskill` 要跑 `gap_split.py`，两份壳里一条 `Bash(python …)` 都没有。
`job-upskill` 那条最难看——它的工作流写着「用工具分，别手数」，然后把唯一那个工具
挡在了权限外面。

三条写法上的约定：

- **前缀形式**：`Bash(python tools/gap_split.py:*)`，不是 `Bash(python tools/gap_split.py *)`。
  后者是精确匹配，只匹配字面量以 ` *` 结尾的那一条命令，永远匹配不上
  （`tests/test_bash_permissions_use_the_prefix_form.py`）。
- **限定到入口，不开宽授权**：写具体那个脚本，别写 `Bash(python tools/:*)`。
  现成的例子是 portal CLI 那两条——都限定到 `cli.ts` 这一个入口。
- **纯咨询壳不申请执行权限**：`job-application-assistant` 只给建议，真要投递时把人
  交给 `/job-apply`（命令 stub 没有 `allowed-tools` 限制）。给它开 typst 权限既没必要，
  也会让人以为这个壳自己会编译 PDF。

查「写全了没有」的是 `tests/test_skill_permissions_cover_its_workflow.py`（上面那条
「顺手看一眼壳」）：从工作流的 ` ```bash ` 块里取命令，对着壳上的规则逐条查。
它**只认代码块**——正文里「Claude Code 本身走 `npm install -g` 安装」是在说别人
怎么装，不是让执行者跑 `npm`。三条写法约定里，第一条由
`tests/test_bash_permissions_use_the_prefix_form.py` 管，后两条靠 review。

> 注意这与 `.claude/settings.json` 是两件事：那份清单是**预批**（免去每次询问），
> 由 `tools/security_guards.py` 的 `ALLOWED_PERMISSIONS` 逐字钉死，**放宽必须显式
> 可审**。壳上的 `allowed-tools` 决定的是「够不够得着」，不是「要不要问你」——
> 给壳补一条权限不等于给它免了询问。

## 代码风格

- **零运行时依赖优先**：portal CLI 只用 `fetch` + 正则/HTML 分片解析，Node 与 Bun 都能直接跑，
  像 `liepin-search` 一样；只有平台的标记确实没法用分片正则解析时才引入解析库，并
  在对应 `README.md` 里说明原因。
- **TDD**：新逻辑先写一个会失败的测试，再写实现让它通过。CLI 测试放
  `.agents/skills/<name>/cli/tests/`（`bun test`，网络无关的部分要能离线跑）；
  Python 工具测试放 `tests/`（`python -m unittest`）。
- **不信任职位描述里的内容**：抓取到的职位文本是不可信输入，任何解析/评估逻辑都不
  应该把它当指令执行，也不应该跟随其中的链接（`SECURITY.md` 详述这条威胁模型）。

## 提交前必跑的三件

本地跑一遍，三个都要退出 0（CI 里就是这三个 job）：

```bash
pip install pyyaml            # 只有 lint_skills.py 要它（解析 frontmatter）
python tools/lint_skills.py
python tools/security_guards.py
python -m unittest discover -s tests -t .
```

> **PyYAML 是这四个检查脚本里唯一的第三方依赖。** 别处（`doctor.py`、
> `security_guards.py`、整个 `tests/`、以及流水线上的每一个 `tools/*.py`）
> 都只用标准库 —— `lint_skills.py` 要读 `SKILL.md` 的 YAML frontmatter，
> 而那里有折叠块标量（`description: >`）。手写一个子集解析器不划算：
> 解析错了这个 lint 会**静默放行**，比装一个包糟得多。
>
> 这条 2026-08-27 补。此前它只写在 CI 里（`.github/workflows/ci.yml` 的
> `pip install pyyaml`），而 README 与 SETUP 都说「只用标准库」—— 
> 新贡献者照着这一节敲，第一条就撞 `requires PyYAML`。

- `lint_skills.py` 检查每个 `SKILL.md`/命令文件的结构（frontmatter、标题格式等），
  以及 `workflows/`（正文权威来源）与 `.claude/commands/` stub 的双向一致性——stub
  必须指向存在的 workflow 且限长 ≤5 行——和 workflows 正文的工具中立性（不得出现
  `WebFetch` 等工具字面量，工具对号统一放在 `AGENTS.md` 能力对照表）。
- `security_guards.py` 检查 `.claude/settings.json` 权限允许列表、`.gitignore` 里
  个人数据保护规则有没有被意外放宽。

涉及 portal CLI 的改动，额外跑一遍对应目录下的 `bun run typecheck` 和 `bun test`。

### ⚠️ 本机这一遍不是「CI 会再跑一次」的重复——个人数据这一档只有它拦得住

个人数据守卫（`tests/test_no_maintainer_data_in_repo.py`）**不在仓库里存任何标识符**：
姓名、雇主、院校、薪资数字、看过评过的公司，全都从你自己的 `profile/` 与职位库
**现取**。这个设计是对的——词表一旦写进仓库，它自己就成了泄漏源。

代价是它**在 CI 上基本不工作**。2026-08-20 实测（把会进提交的那批文件拷进空目录，
没有 `users/`、没有 `.active_user`，也就是 CI 看到的样子）：

```
11 passed, 9 skipped
```

跳掉的 9 条恰好是最要紧的那批——姓名 / 雇主 / 院校 / 个人数字 / 看过评过的公司 /
演示数据是不是拷的真实行 / 守卫自己有没有攒真值。CI 上没有可比对的资料，
它们只能跳过。

**所以：**

> CI 绿 ≠ 没泄漏。**推之前在自己机器上跑一遍全量测试，是唯一一道能挡住
> 个人数据进公开仓库的闸。**

CI 那边只剩一个弱得多的代理判据（`placeholder-integrity`：模板文件里的占位符
还在不在——占位符不见了，通常意味着有人把真实数据写了进去）。它拦得住最粗的
那一类，拦不住「注释里顺手抄了一个真值」。

## 写下一个实测数时，把日期一起写上

这个仓库的规则几乎每一条都挂着一个实测数（「234 份开场白里 24 份踩线」
「85 条投递里 55 条按名字找得到」）。那是它最好的习惯 —— 没有数的规则，
下一版就会被当成洁癖删掉。

**但数会过期，而规则不会。** 语料每涨一批，所有引它的地方同时作废，
而没有任何东西会红。2026-08-23 实测过一次代价：

    开场白       文档写 234 份      现算 236
    踩线         文档写 24 份 10%   现算 37 份 16%
    超 200 字     文档写 1 份 0.4%   现算 0 份

那个 234 被抄进了 **7 处**，三个数全飘了，同一份文档里还同时写着 234 和 236。

所以：

- **带日期的是记录**（「实测活动用户 2026-08-23：…」）。它说的是那天的事，
  过期不算错，**别去「更新」它** —— 那是在拿今天的数抹掉一次真实的观测。
  推导过程尤其要留（「1094 个里 308 个没正文，收窄到两处都空的还剩 18 个」）。

  **例外：它在自己那天就是错的。** 那不是记录，是量错了，照实改。
  分辨很简单 —— 拿它自己写的判据在**那天的数据**上重跑一遍：对不上就是错的。
  实测 2026-08-23 撞到两处：`is_anonymous_employer` 的 docstring 标着当天、
  写「2638 个岗里 1355 个脱敏（51%）、98.5% 是猎头岗」，而同一天同一个判据
  跑出来是 **1369（52%）、97.6%**。

- **改一个数就把同一句里的其它数一起核一遍。** 2026-08-23 把 `greeting_problems`
  的分母从 234 更正到 236 时，同一句的「24 份至少犯一条」忘了跟着改（现算 37）
  —— **一半新一半旧，比两个都旧更难发现**：分母对得上，读的人不会再去查后半句。
- **不带日期的是在说「现在」**。那它就得**现在还成立**：要么给它配一条现算的
  守卫（`test_the_greeting_stats_are_not_stale` 是模板：拿真语料重算，
  没有语料就 `skipTest`，不拿构造数据假装验过），要么补上日期变成记录。

实测 2026-08-23：**63 处不带日期**；棘轮一路收到 2026-08-27 的 **55**。
存量不要求一次改完，`test_measured_numbers_carry_their_date` 用**只降不升的
棘轮**卡住 —— 新写的必须带日期，旧的清一个少一个。

> 棘轮只扫 `workflows/` 和 `tools/`（它的 `SCAN`）。`tests/` 里那些不算 ——
> 测试文件里的实测数几乎全是**事故记录**（「当时那一版实测 41 处命中」），
> 本来就该是历史。想知道全仓有多少，自己按同一个正则扫一遍，别改棘轮的范围：
> 把 `tests/` 并进来，这个数会从 55 跳到一百多，而多出来的那批没有一条该改。

> **为什么不干脆全部改成现算。** 多数数是**当时那一次观测**的产物
> （「那 40 份整整齐齐全缺同一节」），今天重算得不到同一批文件。
> 能现算的才现算，不能的就诚实标上它是哪天的。

---

## 文档和代码打架时，**先判哪边对**，再动手

发现「正文这么写、代码那么做」时，最顺手的动作是**把文档改成迁就代码**——
代码毕竟是在跑的那一份。**这个默认值是错的。** 那一刻要回答的是
「哪一边才是对的」，不是「哪一边在跑」。

实测代价（`portal_budget` 的封控解除，横跨三天）：

| 时间 | 发生了什么 |
|---|---|
| 最早 | 文档写「工具永远不自动解，只有用户手动解封才恢复」 |
| 2026-08-25 | 发现代码一直是 `if now >= t: return NOT_BLOCKED`（到点自动放行）。**把文档改成迁就代码**，还顺手给新说法配了判据、把测试类改了名 |
| 2026-08-26 | 本人裁定：「只有用户手动点继续 cli 后，才能继续 cli」。删掉那一行代码，文档翻回最早那句 |

第 2 步把**一条正确的规则改成了错的**，理由只是「代码是这么写的」。
而那条规则的理由当时就写在旁边（`serve.apply_unblock` 的说明第一行：
「只由人点，工具永远不自动解」，引的是 2026-08-19 的本人裁定）——
**没人读它，因为那天的任务被理解成「消除不一致」**。

所以三条：

- **先找那条规则的理由**（谁定的、为什么、代价是什么）。理由还成立 → 改代码；
  理由过期了 → 改文档，并把「为什么过期」写下来。
- **不确定就问用户**，别自己裁。这一类矛盾多半卡在一个只有他知道的事实上
  （上例：解封的前提是他真去换了 IP / 过了验证，工具看不见）。
- **两边各有一条测试时格外小心**：那说明矛盾已经被制度化了。上例正是这样
  两边全绿地活了一周 —— 不是没人查，是两个查的人各查各的一半。

---

## 判据可能钉着**错的东西**：绿不代表规则对

上一节讲的是「哪一边对」，这一节讲它在测试里的样子：**一条守卫存在，
不构成它守的规则是对的证据。**

2026-08-26 删掉四个凭空拍出来的抓取额度（每轮 10 次动作、每天 2 轮、
轮间隔 10 分、每天 60 次请求）时，**39 条测试当场变红**。它们每一条都写得很扎实
——有变异验证、有实测数据、有事故复盘——只是守的那件事本身没有依据：
那四个数没有一个来自平台，全是拿某次观测打三折得来的。

更值得记的是**它们红得毫无区分度**：真规则（撞了风控要停）和假规则
（本轮到 10 次要停）一起红，看不出哪条该修、哪条该删。所以：

- **守卫的说明里要写清「这个数是哪来的」** —— 平台返回的、用户裁定的、
  还是我们自己定的安全边距。第三种要显式标出来，它是最该被质疑的一档。
- **改一条规则时逐个看红掉的判据**，分三堆：跟着改（量纲变了）、
  反过来写（结论反了）、整条删（它守的东西没有了）。**别整批改成绿。**
- 删掉一条判据时，**在原地留一句它为什么没有了**（本轮删的四条都留了）。
  否则下一个人只会看到覆盖率掉了一块。

---

## 判据钉**行为**，不钉那一行代码长什么样

上一节讲「守卫可能守错东西」，这一节讲它另一种失效：**守对了东西，却钉在
它当下住的那个位置上。** 判据一旦写成「某个函数体里有没有这一行」，
一次合理的搬家就会让它红 —— 而红的不是回归，是重构本身。

2026-08-31 把收尾那两个集合的记账从 `live_tail` 搬进 `_cli.note_live`
（手写尾巴的两条检查也要调它），**三条断言同时红**，三条钉的都是位置：

    查 `live_tail` 体内有没有 `LIVE_REWRITE`
    查那一行 `LIVE_REWRITE.update(...)` 长什么样
    查 `_cli.py` 源码里有没有 `LIVE_SEEN.update`

它们守的规则都对（去重只有一本账、值要留原样链接），只是问法错了。改成
「调一次 `note_live`，看集合里有什么」之后，同一批变异照样全红。

同一课这个仓库交过好几次：`test_the_copies_in_doctor_still_match` 的开头
写着「守卫钉实现位置，等于把重构本身判成违规」；`test_the_maybe_tier_is_two_things`、
`test_cli_contract` 也各栽过一次。

**怎么分**：needle 是**跨模块的名字**（`_cli.claim_without_source(`）时，
钉的是「这个消费方走不走正本」—— 那是契约，该钉。needle 是**局部实现**
（`skipped.add(`、`real = [n for n in users`）时，钉的是位置。
实测 2026-08-31：对源码文本的 `assertIn` 共 1667 条，其中 56 条是前者、
**228 条是后者**。后者由 `test_a_guard_pins_behaviour_not_a_line` 立了一条
只降不升的上限 —— 不要求清零（TS 组件那类在 Python 里执行不了，只能扫源码），
要求的是别再涨。

---

## 改持久化数据的**语义**时，老记录按写它时的规矩读

改的不是字段名而是**同一个字段的含义**时，盘上那些旧记录是按旧含义写下的。
一视同仁地按新含义读，等于改写历史。

实测（2026-08-26，封控从「满 24 小时自动放行」改成「只有人能放行」）：
一条 **161 小时前**的 BOSS 记录当场把 BOSS 整家判停 —— 而它在旧语义下早就
到点释放过了，同一天我还用浏览器读完了它 6 个职位页。

解法是给新记录加一个**旧记录不可能有的字段**（这里是 `since`：撞上的时刻），
拿它当分水岭：没有 = 旧记录，照旧规矩；有 = 新记录，照新规矩。
两边都要有判据钉着，否则下一轮重构时那个分支会被当成死代码删掉。

---

## 写守卫（tests/ 里扫真实数据或源码的测试）必须做变异验证

写完守卫先**故意把实现改回错的样子**，确认它红；还原，确认它绿。绿-红-绿三步
缺一不可——只看它绿过，等于没验过。

这条不是洁癖。2026-08-13 一天全面检查十轮，**每一轮的真缺陷都出在守卫身上，
没有一个在功能代码**，而且失效方式高度重复，全是「测了个容易测的东西，
不是要守的东西」：

| 失效形状 | 实测案例 |
|---|---|
| 精确匹配自由文本的一种写法 | `来源` 有 15 种写法，守卫只认 1 种，1246 条里查了 1 条 |
| 只比常量，不比函数行为 | 两个元组是子集，两个函数一个 `startswith` 一个精确 `in` |
| 注释说同源，没有断言比过 | `SHORTLIST_FLOOR` 与前端 `MATERIALS_FLOOR` |
| 取样范围比声称的窄 | 取样函数只覆盖 29% 的库，880 条静默漏过 |
| 只查一半的目录 / 单向比对 | 白名单只验 Python ⊆ TS，反向多一个词照绿 |
| 断言绑死一种写法而非规则 | `assertIn('ex.main([...])')`，正当重构报假警 |
| **停在「东西在不在」，没走到「它对不对」** | `hasattr(fd, "missing_by_portal")`；`read_text` 确认「解析只有一份」却从没执行过它；把要测的函数打桩换成 lambda |
| **断言撞上解释自己的文字** | 禁掉某个字符串的守卫，被**讲这条规则的注释**引到，于是在变异之前就红。2026-08-26 第七次出现（`tests/test_only_a_person_lifts_a_block.py` 禁「等一段时间再跑」，而禁令自己引用了它）。解法固定：扫之前先剥掉注释与 docstring （`re.sub(r'"""[\s\S]*?"""|#[^
]*', "", raw)`）—— 规则总得能解释自己 |

这六种没有一种能靠再写一条守卫**彻底**杜绝（守卫守不了「守卫有意义」这件事），
唯一可靠的检法就是变异验证——所以它是纪律，不是建议。变异还要**有效**：
确认注入的错真的会落到你断言的那条路径上（实测有过变异根本不落盘、测试照绿，
白验了一轮）。

顺手的三条结构性规矩，能把上表里三种形状直接消掉：

- **路由/分流字段是枚举不是自由文本**——大类前缀固定，新写法进不来（`来源` 一节的教训）；
- **按字段分流的守卫必须管住「字段缺失」那一支**——空值不匹配任何前缀，会整批静默漏过；
- **同一个数只定义一处**，另一处要用就写断言钉住相等，别只写注释说「同源」。

## 怎么先找到「还没被守住」的地方：换尺子，不是再读一遍代码

上一节讲守卫**怎么失效**，这一节讲怎么**先发现**哪里没被守住。

2026-08-20/21 连着用了**八种**查法，产出差得很远，而差别与「代码看起来对不对」几乎无关，
只与「**这把尺子以前有没有量过**」有关：

| 查法 | 产出 | 适合找什么 |
|---|---|---|
| 读判据、找说明里的自承缺口 | 0 —— 全是叙述历史或已定论的取舍 | 已经被想过的问题 |
| 对着真实职位库跑 `tools/audit_pipeline.py` | 0 新的 | 数据层面的规则漏洞 |
| **量 `tools/` 的行覆盖率** | **9 处** | **判据停在「东西在不在」的那一类** |
| **读产品真正交付的产出** | **5 处** | **模板里的字被逐字抄给用户、报告自己少报** |
| AST 扫「恒真的断言」 | 0（尺子验过会亮） | 断言写了却不可能失败 |
| 扫文档里写的工具调用与开关 | 0 —— **而且发现早就有判据在管**（`ToolFlagsResolve`） | 教用户敲一个工具不认的参数 |
| 扫「定义了没人调用」的函数 | 0 —— **故意不固化**（启发式，会误报） | 规则声明了、另一头没落点 |
| 产物的生产者/消费者图 | 0 | 同上，从数据这一侧看 |
| **像执行者一样通读一份工作流** | **2 处** | **步骤顺序、伪代码与表自相矛盾** |
| **把 markdown 渲染出来看** | **3 处** | **块结构：源码逐行都对，渲染出来是另一回事** |

**产出最高的两把是「量执行」和「读产出」——都不是读代码。**
行覆盖率那九处没有一处是「忘了写测试」，全是测试写了、但没走到要测的东西：
把函数打桩换掉、只 `hasattr` 加源码文本比对、只验链条的形状不验它通不通、
夹具里恰好没有触发那一支的数据。**这层差别读代码看不出来，只有量执行才看得见。**

没装 `coverage` 也能量：Python 3.12+ 的 `sys.monitoring` 可以只给 `tools/` 里的
code object 装 `LINE` 钩子（`set_local_events`），别处零开销，整套测试跑完只慢一点。

> **这个探针本仓库不附带**（约 60 行，一次性用完就丢，不值得当成要维护的工具）。
> 要重建它：import `tools/*.py` → 递归收集它们的 code object → 逐个
> `set_local_events(tool_id, code, LINE)` → 原地 `unittest.discover` 跑一遍 →
> 拿 `code.co_lines()` 的行号集合减去命中集合。回调里 `return sys.monitoring.DISABLE`，
> 命中一次就够。

### 读产出：最容易被跳过、产出却最高的一把

前面几把量的都是「我们写的东西对不对」。**读产出量的是「它产出的东西长什么样」**——
把工具真的生成的文件拿出来数一遍。2026-08-21 扫六类产出（投递话术 236 份、
评估 268 份、JD 快照 284 份、投递台账 83 行、简历审计、学习计划），出了 5 处修补，
形状高度一致：

- **模板里出现的字会被逐字抄给用户**（话术抬头写着「判词」、简历审计的小节名写着
  「能力边界」——都是 `AGENTS.md` 明令不上台面的框架词）；
- **报告自己在少报**（审计的漏判统计写死 `most_common(3)`，七道门只报三道）；
- **数出来是 0 的那一格最值钱**（某个归档文件 285 个目录里 0 份，
  顺着查出它在仓库里有**两个名字**，写它的和读它的永远碰不上头）。

**「产出为空」不报错、不缺页，只是那一格是 0——要去数它，才看得见。**

### 用仪器之前先证明它会亮

覆盖率工具最危险的失效方式是**少报**——把「测过的」说成「没测过」，
于是你去补一堆本来就有的测试，还以为找到了缺口。所以先自检：
钩住一个函数、真调一次、确认记到了行。扫描类的工具同理——先喂一份**已知有问题**的
输入，确认它报出来，再信它的 0。

读数还要会打折：`def` 那一行在装钩子之前就执行过（假象）；`main()` 的 argparse
由 `--help` 的子进程测试覆盖（假象）；`lint_skills` / `security_guards` /
`verify_pdf` / `doctor` 的测试整体走子进程，进程内量不到（假象）。
**滤掉这三类之后剩下的才是真空白。**

### 变异测试自己翻车的三种方式

上一节说变异要「有效」。实测还有三种更蠢但更常见的翻法，都会让你把没验过的当成验过：

- **`errors` 不是 `failures`。** 类名/路径写错时 unittest 报
  `FAILED (errors=1)`——那是「找不到这个测试」，不是判据抓到了东西。
  **只有 failure 才算变异被抓住。**
- **变异把模块改成语法错误。** 往 f-string 的表达式里塞 `# 注释` 就会；
  模块根本没导入成功，而如果你只 grep `^(Ran |OK|FAILED)`，
  **什么都匹配不到，看起来像「跑了但没红」**。
  变异之后先确认「模块还能导入、测试真的跑了」，再看它红不红。
- **连着改同一个 `.py` 时，`.pyc` 会给你一个假红。** 前两种是「没红当成红」，
  这一种反过来：**跑的根本不是你刚写进去的代码**。CPython 判断字节码缓存新不新
  用的是源文件的 mtime，而它的粒度撑不住「一秒内连改两次」——批量跑变异正好是
  这个节奏，于是第二个变异跑的是第一个变异编译出来的 `.pyc`。
  实测（2026-08-23）：同一个常量在 60/70/80 三个取值上各跑三轮，
  **80 那一档绿一次红两次**，而它本该稳定绿；加上 `PYTHONDONTWRITEBYTECODE=1`
  之后九次全部一致。
  **凡是变异改到可执行代码（不只是注释和 docstring）的批量脚本，
  一律带 `PYTHONDONTWRITEBYTECODE=1` 跑。** 改文档字符串不受影响 ——
  那类断言读的是源码文本，不是字节码。

  **它还会活过脚本本身。** 2026-08-26 实测：把 `max(known)` 换成 `min(known)`
  验一条守卫，脚本跑完、源码还原、`git status` 干净、那两行 `grep` 出来也对 ——
  而**之后每一次单独跑那个测试仍然是红的**，跑的是残留的 `.pyc`。
  差点据此去「修」一个并不存在的 bug。
  这一次特别难认，因为 `min`/`max` **长度相同**：CPython 判缓存新不新看的是
  源文件的 mtime（秒）**加大小**，长度一变就必然重编译，而单字符对调两样都没变。
  单字符对调（`>=`↔`>`、`and`↔`or`、`min`↔`max`）正是最常用的变异形状。

  **认它的办法是别信 `git status`** —— 源码干净不代表跑的是源码。
  测试红得莫名其妙时，先 `find . -name __pycache__ -type d -exec rm -rf {} +`
  再判断。

### 恢复变异**绝不能用 git**

`git checkout -- <file>` 不是「撤销我刚才那个变异」，是「把这个文件退回 HEAD」。
工作树里有未提交的改动时，**它会连着那些改动一起抹掉**。

2026-08-21 实测代价：为了验一条判据，把某份工作流变异之后用
`git checkout -- workflows/job-setup.md` 恢复——**229 行未提交的翻译当场消失**。
（找回来了：干净 clone 实测留下的那份拷贝正好是翻译之后建的。**那是运气，不是流程。**）

正确的做法只有两种：

- **先把原文存起来**（变量里、临时文件里），恢复时写回去；
- 或者用**精确字符串替换回去**——变异改了什么就换回什么，别动别的。

改完一定要**另跑一条命令独立复查**文件已还原（`grep -c` 那个标记 + 语法检查），
不要信自己「我恢复过了」——本仓库有过变异脚本挂在超时里、恢复那一步根本没执行的实测。

### 造新尺子之前先搜一遍

**本轮实测代价**：为「文档里写的 `--flag` 工具认不认」造了一条新判据，
写完、变异验过、记录写完，才发现 `test_cross_references_resolve.ToolFlagsResolve`
**早就在做同一件事**，而且做得更好——它用子进程跑真 `--help`，
所以像 `--user` 这种由 `_cli.resolve_user` 统一加、源码里搜不到的开关**天然就被认出来**，
不需要我那个 AST 特例。新判据当场删掉。

漏搜的原因很具体：我只查了名字最像的 `test_index_matches_the_workflows`
（它管的是**斜杠命令**的开关），就断定「shell 里敲的那行没人管」。
**判据的文件名不按它守的东西命名**，`grep` 关键词才是可靠的入口——
搜 `python tools/`、搜那个概念本身，别搜你以为它该叫什么。

**搜到了要打开看，别只读命中行。** 2026-08-21 第四次撞车就栽在这儿：
搜 `n_processed` 时 `test_pipeline_counts.py:110` 就在结果里，那一行写着
「第一版调的是 `doctor._n_processed(seen)`」——看着像历史注释，就没打开文件。
打开才会看到下一句：**「验了零件，没验那条路」**，而那条既有判据跑的正是整条路，
比我要新建的那条强。**命中行是索引，不是结论。**

**自查重复本身就是一种找 bug 的手段。** 要判断「是不是重复」，
就必须去读那条既有判据**到底守什么**——而它守得往往比它自己以为的窄。
2026-08-21 实测：查重时读 `test_one_file_one_tolerance` 那张手工表，
发现它漏了 `doctor`；顺着把判据从只抓裸 `["seen"]` 加宽到也抓 `.get("seen", {})`，
**当场又抓到 `build_dashboard` 与 `gap_split` 两处同类 bug**。
查重那一趟的产出，比原本要新建的那条判据大得多。

还有一种「看着重复其实不是」：**两条测的是同一件事的两个实现**。
`test_dashboard_matching` 测 `build_model`（只有测试在调的夹具），
`test_materials_and_status_reach_the_panel` 测 `export_web_data.main()`（真跑的那条）。
**两条都要留，而且要互相点名**——否则下一个人会当重复删掉其中一条，
而被删的很可能正是测生产路径的那条。

**同一天犯了两次**：又为「读 JD 的命令要复述信任边界」新建了一条，
而 `EveryJdReaderRestatesTheTrustBoundary` 早就在做——类名就是那句话。
第二次的收获比第一次大：那条判据的名单是**手工维护**的（注释写着「记得往这里加」），
而它正是靠这一点漏掉了两条命令。**手工名单 + 一句「记得加」= 迟早会漏**，
所以并入时给它补了一条推导校验，而不是只补两个名字。

### 最后一把：像执行者一样通读

前面几把都是机械扫描，而**扫描预设了「问题长什么样」**。有一类问题没有形状可扫：
**步骤顺序**。

2026-08-21 通读 `/job-auto` 的循环时读出来的：`--target` 的跳出判据排在「补货」之后，
而 `/job-rank` 会评到排空、补货那一支以「回到 1」收尾——**那一步永远够不着**，
`--target 20` 攒够了照样继续抓。同一份文件的停手条件表里却明写着
「达到 `--target` → 正常结束」。八把机械尺子一把都没扫出来。

**读的时候把自己当成照着做的那个执行者**，一步一步走：这一步之后我在哪？
下一步够得着吗？这个分支有出口吗？——比「这句话对不对」有用得多。

### 第十把：把 markdown 渲染出来看

前九把量的都是**源码**。而 markdown 有一层源码里看不见的东西：**块结构**。

2026-08-21 实测三处，形状各不相同，共同点是**逐行读全都对**：

| 文件 | 源码里 | 渲染出来 |
|---|---|---|
| `job-auto.md` | 「停手条件」9 行的表，中间插了段 `> ⚠️ …` | 表只剩 2 行；**另外 7 条**（含「用户喊停」「达到 `--target`」）被块引用吸成一段带竖线的灰字 |
| `cdp-portals.md` | 额度表的后 3 行 | 被两个后插的 `###` 小节冲散，成了没有表头的孤儿行 |
| `job-scrape.md` | 猎聘那格写 `` `
` `` `` `
` `` `` `	` `` | **代码格里装的是真控制字符**，一行劈成三行：表格多出 2 条空行、加粗没闭合、正文断在半句 |

第三处最值得记：它在 `HEAD` 里就是坏的，而且成因就是本仓库反复栽的
**unquoted heredoc 吃掉一层反斜杠**——`
` 落盘时成了真换行。
写 markdown 里的转义序列时用 `chr(92)` 拼，或者用 Write 工具。

**两把尺子要一起用，它们互相漏。** 真渲染（`markdown` 库）漏掉了第三处
——python-markdown 把那两行吸成了空表格行，没留下裸竖线给探针抓；
而按 GFM 块规则自己走一遍的那份复刻抓到了它。反过来，复刻不管块引用里的表。
判据落在 `tests/test_tables_survive_rendering.py`（**不 import `markdown`**：
本仓库对外声明只用标准库）。

> **借别人的枚举器之前先看它量的是哪一段。** 这条判据第一版复用了
> `test_docs_track_the_framework._all_docs`，而那个函数的 SKIP 里带着
> `workflows`——于是判据只扫了 18 份，**三处发现所在的目录一份都没扫到**，
> 全绿。是变异验证（把真文件改回坏的，看判据亮不亮）才把仪器自己的毛病翻出来。
>
> 现在那条判据带一个**份数下限**（`FLOOR = 60`）。全绿有两种可能——
> 真没问题，或者根本没扫到东西；下限把后者变成红。**任何全仓扫描都该带一条。**

### 什么时候该停

不是「找不到问题了」——那句话在这一轮被推翻过两次，两次都是**换把尺子就又挖出东西**。

真正的信号是**信噪比翻转**：最后三把尺子里有两次「发现」其实是尺子自己的 bug
（孤儿扫描的正则把 `模块.名字` 的调用全排除掉；产物图的写关键词表漏了「输出」）。
**当误报开始多于真发现，继续造尺子的成本就高于它的期望产出了。**

`documents/`、`job_search_tracker.csv`、
`documents/applications/**` 等含个人求职资料的路径已经在 `.gitignore` 里，不要把它
们的内容提交进仓库；这条规则由 `security_guards.py` 兜底检查。
