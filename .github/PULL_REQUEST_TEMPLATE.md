## 改了什么、为什么

## 触发场景 / 复现步骤（修复类必填）

## 验证
<!-- 按 CONTRIBUTING.md，本地这三个都要退出 0：
     python tools/lint_skills.py
     python tools/security_guards.py
     python -m unittest discover -s tests -t .
     改了 portal CLI 的话，再在对应目录跑 bun run typecheck 与 bun test -->
